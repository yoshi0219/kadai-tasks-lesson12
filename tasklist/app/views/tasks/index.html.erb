<h1>List of tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.id, task %> : <%= task.title %>  &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= link_to 'Create a new task', new_task_path %>